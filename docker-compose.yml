services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: oskar
      POSTGRES_PASSWORD: barcelona123
      POSTGRES_DB: barcelona_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: